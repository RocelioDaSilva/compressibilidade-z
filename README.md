# compressibilidade z

Repository skeleton for my projects.

Structure
- `projects/` — your project folders
- `docs/` — documentation
- `scripts/` — helper scripts

Next steps:
- Add project folders inside `projects/`.
- Optionally add a remote and push.
